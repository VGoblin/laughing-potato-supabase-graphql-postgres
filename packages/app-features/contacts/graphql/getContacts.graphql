query GetContacts($type: String) {
  contactsCollection {
    edges {
      node {
        id
        firstName
        lastName
        fullName
        email
        status
        createdAt
        updatedAt
      }
    }
  }
}
